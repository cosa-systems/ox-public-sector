apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ox-common.names.fullname" . }}-test-connection"
  labels:
    {{- include "ox-common.labels.standard" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ox-common.names.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
